cargo-features = ["strip"]

[workspace]
members = [
  "oxicrypt-core",
  "oxicrypt-sys",
  "oxicrypt",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = "symbols"
